% Glue: transition 12 — authored from the wiki edges between its
% neighbouring chapters (docs/wiki/INDEX.md); the roadmap marks progress.
\cleardoublepage
\begin{narrative}
\section*{The road so far}

\roadmap{12}

\noindent The walker knows how to step. What it needs now is the slope of THE loss — the alignment sum over \\anchor{001.paths15} paths — and a sum too wide to list needs its slope organised as cleverly as its value was. The next chapter is the destination: the trellis swept backward, every cell priced by the paths through it, and the gradient collapsing to one subtraction — softmax minus how often the truth used each cell.
\end{narrative}
