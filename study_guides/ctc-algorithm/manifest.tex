% The retrieval manifest for this objective — the ordered \primitive calls
% ARE the remix map's spine (plan 012 R6); both build targets (guide and
% solutions manual) input this one file, so the stitching is single-sourced.
%
% Phase-2+ order (full comprehension of the CTC algorithm):
%   counting-rules, ctc-alignment, dynamic-programming, independence,
%   conditional-probability, bayes-rule, logarithms, e-and-ln,
%   random-variables, softmax-likelihood, derivative-toolkit,
%   gradient-descent, ctc-gradient, ctc-decoding
% Glue transitions interleave here as \input{glue-NN} once authored.

\primitive{counting-rules}
